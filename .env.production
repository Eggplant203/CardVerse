# Production Environment Variables for Netlify
# Copy these to Netlify Environment Variables (Site Settings > Environment Variables)
# Gemini API Key for image analysis
NEXT_PUBLIC_GEMINI_API_KEY=${NEXT_PUBLIC_GEMINI_API_KEY}

# Database Configuration (use external database like Neon, Supabase, etc.)
DATABASE_URL=${DATABASE_URL}
DIRECT_URL=${DIRECT_URL}

# JWT Settings (generate new secrets for production)
JWT_SECRET=${JWT_SECRET}
REFRESH_SECRET=${REFRESH_SECRET}
JWT_EXPIRY=15m
REFRESH_EXPIRY=7d

# Email Settings (configure for production email service)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=cardverse.eggplant203@gmail.com
EMAIL_PASS=${EMAIL_PASS}
FROM_EMAIL=CardVerse Support <cardverse.eggplant203@gmail.com>

# Application URLs (will be set automatically by Netlify)
NEXT_PUBLIC_BASE_URL=${URL}
